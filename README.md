# QuickSearch

A complete rewrite of [IMB11's Codex](https://modrinth.com/mod/codex) for 1.21.10.

Press `N` to open the search bar.

### Developing
Please copy the `hooks` directory into `.git` so you don't accidentally commit versions other than 1.21.10. Thanks!
```bash
cp -r hooks .git
```